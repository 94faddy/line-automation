<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LINE Automation v4.0</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>
<body>

<!-- Top Control Bar -->
<div class="control-bar">
  <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
    <div class="d-flex align-items-center gap-3">
      <h5 class="text-white mb-0">
        <i class="bi bi-chat-dots-fill text-success"></i> LINE Automation v4
      </h5>
    </div>
    <div class="d-flex gap-2 flex-wrap">
      <button class="btn btn-success" onclick="startSending()" id="btn-start">
        <i class="bi bi-send-fill"></i> ส่ง
      </button>
      <button class="btn btn-warning" onclick="pauseAll()" id="btn-pause-all">
        <i class="bi bi-pause-fill"></i> พักทั้งหมด
      </button>
      <button class="btn btn-info" onclick="resumeAll()" id="btn-resume-all">
        <i class="bi bi-play-fill"></i> ต่อทั้งหมด
      </button>
      <button class="btn btn-danger" onclick="stopAll()" id="btn-stop-all">
        <i class="bi bi-stop-fill"></i> หยุดทั้งหมด
      </button>
      <button class="btn btn-secondary" onclick="resetAll()" id="btn-reset-all">
        <i class="bi bi-arrow-counterclockwise"></i>
      </button>
      <button class="btn btn-outline-light" onclick="refreshInstances()" id="btn-refresh" title="Refresh">
        <i class="bi bi-arrow-clockwise"></i>
      </button>
    </div>
  </div>
</div>

<!-- Main Content -->
<div class="main-container">
  
  <!-- Left Panel - Settings -->
  <div class="settings-panel">
    
    <!-- Message Card -->
    <div class="card">
      <div class="card-header">
        <i class="bi bi-chat-text"></i> ข้อความ
      </div>
      <div class="card-body">
        <textarea class="form-control message-input" id="message" rows="4" 
                  placeholder="พิมพ์ข้อความที่นี่...">สวัสดีครับ! 👋

ข้อความทดสอบจาก LINE automation
ลิงก์: https://example.com</textarea>
      </div>
    </div>
    
    <!-- Settings Card -->
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <span><i class="bi bi-gear"></i> ตั้งค่า</span>
        <div class="form-check form-switch mb-0">
          <input class="form-check-input" type="checkbox" id="sendAllFriends" checked>
          <label class="form-check-label" for="sendAllFriends">ส่งให้เพื่อนทั้งหมด</label>
        </div>
      </div>
      <div class="card-body">
        
        <div id="send-all-info" class="alert alert-success mb-3 py-2">
          <i class="bi bi-people-fill"></i> จะส่งให้ <strong>เพื่อนทั้งหมด</strong> ในแต่ละ instance
        </div>
        
        <div class="row g-2 mb-3">
          <div class="col-6">
            <label class="form-label small"><i class="bi bi-speedometer2"></i> ความเร็ว</label>
            <div class="btn-group w-100">
              <input type="radio" class="btn-check" name="speed" id="speed-normal" value="normal">
              <label class="btn btn-outline-secondary btn-sm speed-btn" for="speed-normal">x1</label>
              
              <input type="radio" class="btn-check" name="speed" id="speed-fast" value="fast" checked>
              <label class="btn btn-outline-secondary btn-sm speed-btn" for="speed-fast">x2</label>
              
              <input type="radio" class="btn-check" name="speed" id="speed-turbo" value="turbo">
              <label class="btn btn-outline-secondary btn-sm speed-btn" for="speed-turbo">x3</label>
            </div>
          </div>
          <div class="col-6">
            <label class="form-label small"><i class="bi bi-diagram-3"></i> โหมดส่ง</label>
            <select class="form-select form-select-sm" id="send-mode">
              <option value="parallel">ส่งพร้อมกัน</option>
              <option value="sequential">ส่งทีละตัว</option>
            </select>
          </div>
        </div>
        
        <!-- Instance Selection -->
        <div class="d-flex justify-content-between align-items-center mb-2">
          <label class="form-label mb-0 small"><i class="bi bi-phone"></i> เลือก Instance</label>
          <div>
            <button class="btn btn-outline-primary btn-sm py-0" onclick="selectAllInstances()">เลือกทั้งหมด</button>
            <button class="btn btn-outline-secondary btn-sm py-0" onclick="selectNoneInstances()">ไม่เลือก</button>
          </div>
        </div>
        
      </div>
    </div>
    
    <!-- Logs Card -->
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center py-2">
        <span><i class="bi bi-terminal"></i> Logs</span>
        <button class="btn btn-sm btn-outline-secondary py-0" onclick="clearLogs()">
          <i class="bi bi-trash"></i>
        </button>
      </div>
      <div class="card-body p-0">
        <div class="log-box" id="log-box" style="height: 200px;">
          <p class="text-muted">รอ logs...</p>
        </div>
      </div>
    </div>
    
  </div>
  
  <!-- Right Panel - Instance Cards -->
  <div class="instances-panel">
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <span><i class="bi bi-phone"></i> Instances</span>
        <small class="text-muted">แต่ละ instance มี progress แยก</small>
      </div>
      <div class="card-body p-2" id="instance-cards">
        <!-- Instance cards will be rendered here -->
        <div class="text-center text-muted py-4">
          <i class="bi bi-arrow-clockwise"></i> กำลังโหลด...
        </div>
      </div>
    </div>
  </div>
  
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/socket.io/socket.io.js"></script>
<script>
  // Config จาก server
  window.APP_CONFIG = {
    autoRefreshInterval: <%= typeof autoRefreshInterval !== 'undefined' ? autoRefreshInterval : 5 %>
  };
</script>
<script src="/js/app.js"></script>
</body>
</html>
